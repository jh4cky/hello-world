pipeline {
  agent {label 'centos7'}

  stages {
    stage('Hello') {
      steps {
        sh '''
          ansible-playbook playbooks/os.yaml -i inventory/hosts-ansibl  -u root -e "ansible_password=emergenza"
        '''
       }
     }
   }
 }
